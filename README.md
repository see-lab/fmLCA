# lca-fmu

The python fmus are build using syntax like `pythonfmu build -f power_sink.py` from within the `powersink_fmu` syntax. Needs refinement, though.

# Credits
Code written by  Fitz Koch: fkeenank@uvm.edu
